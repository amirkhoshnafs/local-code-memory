# Test-Driven Fix Prompt

Use `@AI_MEMORY.md` as project memory.

The tests are failing. Use the attached files and exact failing output.

Rules:
- Identify the failing behavior first.
- Fix only the relevant file.
- Do not modify tests unless I explicitly ask.
- Keep the change targeted.
- Preserve existing style.
- After the fix, tell me the exact command to run.

Failing output:

```text
<paste traceback here>
```

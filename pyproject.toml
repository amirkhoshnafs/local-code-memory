[project]
name = "local-code-memory"
version = "0.1.0"
description = "A lightweight project-memory workflow for coding assistants."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}

[tool.pytest.ini_options]
pythonpath = ["."]
